{
  "name": "voxvidia",
  "version": "1.0.0",
  "private": true,
  "description": "PersonaPlex voice agent system — Twilio bridge, tool orchestrator, post-call analytics",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @voxvidia/bridge dev",
    "start": "pnpm --filter @voxvidia/bridge start",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=20"
  }
}
